version: "3"
services:
  app:
    logging:
      driver: none
  worker:
    logging:
      driver: none
    environment:
      MONGODB_CONN: mongodb+srv://dbUser:XwgmOHe0WNkQ87YN@cluster0.nvpuq.mongodb.net/pith-test?retryWrites=true&w=majority
      MONGODB_NAME: pith-test
  tests:
    build:
      context: .
      dockerfile: tests.Dockerfile
    depends_on:
      - common
      - app
      - worker
      - mongo
      - redis
    environment:
      MONGODB_CONN: mongodb+srv://dbUser:XwgmOHe0WNkQ87YN@cluster0.nvpuq.mongodb.net/pith-test?retryWrites=true&w=majority
      MONGODB_NAME: pith-test
      REDIS_IP: redis
