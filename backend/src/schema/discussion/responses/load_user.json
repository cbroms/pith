{
  "type": "object",
  "properties": {
    "cursors": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "user_id": {"type": "string"},
          "nickname": {"type": "string"},
          "cursor": {
            "type": "object",
            "properties": {
              "unit_id": {"type": "string"},
              "position": {"type": "integer", "minimum": -1}
            },
            "required": ["unit_id", "position"]
          }
        },
        "required": ["user_id", "nickname", "cursor"]
      }
    },
    "current_unit": {"type": "string"},
    "timeline": {
      "type" : "array",
      "items": {
        "type": "object",
        "properties": {
          "unit_id": {"type": "string"},
          "pith": {"type": "string"},
          "start_time": {"type": "string"},
          "end_time": {"type": "string"}
        },
        "required": ["unit_id", "pith", "start_time", "end_time"]
      }
    }, 
    "chat_history": {
      "type": "array",
      "items": {"type": "string"}
    },
    "chat_map": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "unit_id": {"type": "string"},
          "pith": {"type": "string"},
          "author": {"type": "string"},
          "created_at": {"type": "string"}
        },
        "required": ["unit_id", "pith", "author", "created_at"]
      }
    }
  },
  "required": ["cursors", "current_unit", "timeline", "chat_history", "chat_map"]
}

